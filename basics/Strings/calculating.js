//to calculate no of letters

const stTrim = " I am from machilipatnam  ";
console.log(stTrim.length);


//to calculate no.of words from the line without spaces
const resTrim = stTrim.trim();
console.log(resTrim.length);

//to display the required letters/words from the word by using arrays
const arr = [01,02,21,24,27,29,51,52];
console.log(arr.slice(1,7));

const s = 145732;
const sRes = s.toString();
console.log(sRes, typeof sRes);