// to replace the word from the line
const strRep = 'I am a designer';
const resOfReplace = strRep.replace('designer', 'superman');
console.log(resOfReplace);

// to convert the small case to upper case from the line
const strRep1 = 'I am a designer';
console.log(strRep1.toUpperCase());

// to convert the upper case to lower case from the line
const strRep2 = 'I AM A DESIGNER';
console.log(strRep2.toLowerCase());

//local lowercase
const strRep3 = 'I AM A DESIGNER';
console.log(strRep3.toLocaleLowerCase());

//local uppercase
const strRep4 = 'I am a designer';
console.log(strRep4.toLocaleUpperCase());



