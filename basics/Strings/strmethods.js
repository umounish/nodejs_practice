const str = "mounish"

// to eliminate the last letter from the required word using slice method

const res = str.slice(1,6);
console.log(res);

// to write the word from our required position of word by using slice method
const res2 = str.slice(1);
console.log(res2);

// to eliminate the last letter from the required word by using substring method

const resOfSubstring = str.substring(2,6);
console.log(resOfSubstring);

// to write the word from our required position of word using substring method

const resOfSubstring2 = str.substring(3);
console.log(resOfSubstring2);
