// writing a program to check weather the string is palindrome or not

       // process: Step 1. need to split the existing string
        //        Step 2. need to reverse the existing string
      //         Step 3. compare the present string with the reversed string
     //         Step 4. if both strings are match, then given string is a palindrome


     // string

const str = 'kotak';

// Step 1. need to split the existing string

const strsplit = str.split(``);

console.log(strsplit);

