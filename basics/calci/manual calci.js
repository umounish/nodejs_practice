const cal = function cal (x, y, oper) {
    const sum = x + y;
    const subtraction = x - y;
    const multiply = x * y;
    const modulus = x % y;
    }
   const res = cal();
   console.log(process.argv2)


